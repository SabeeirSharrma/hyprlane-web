---
import Layout from '../../layouts/Layout.astro';
const base = import.meta.env.BASE_URL;
---

<Layout title="Dashboard" description="Manage your server's Hyprlane verification">
  <div class="flex min-h-screen">
    <aside id="sidebar" class="w-56 bg-bg-surface border-r border-border-subtle fixed h-full flex flex-col hidden">
      <div class="p-4 border-b border-border-subtle">
        <a href={`${import.meta.env.BASE_URL}`} class="font-display text-lg font-semibold text-text-primary hover:text-accent transition-colors">Hyprlane</a>
      </div>
      <div id="guild-header" class="hidden px-4 py-3 border-b border-border-subtle">
        <div class="flex items-center gap-3">
          <div id="guild-icon" class="w-8 h-8 rounded-full bg-accent/15 flex items-center justify-center text-accent font-display text-sm font-semibold flex-shrink-0"></div>
          <div class="min-w-0">
            <p id="guild-name" class="text-sm font-medium truncate text-text-primary"></p>
            <p id="guild-id-display" class="text-xs text-text-muted font-mono truncate"></p>
          </div>
        </div>
      </div>
      <nav id="sidebar-nav" class="flex-1 p-3 flex flex-col gap-1"></nav>
    </aside>

    <main id="main" class="flex-1 p-8">
      <div id="view-login" class="flex items-center justify-center min-h-[70vh]">
        <div class="text-center">
          <h1 class="font-display text-3xl font-semibold mb-4">Server Dashboard</h1>
          <p class="text-text-secondary mb-8">Log in with Discord to manage your server.</p>
          <button id="login-btn" class="btn-primary">
            <svg class="w-5 h-5" viewBox="0 0 24 24" fill="currentColor"><path d="M20.317 4.37a19.791 19.791 0 0 0-4.885-1.515.074.074 0 0 0-.079.037c-.21.375-.444.864-.608 1.25a18.27 18.27 0 0 0-5.487 0 12.64 12.64 0 0 0-.617-1.25.077.077 0 0 0-.079-.037A19.736 19.736 0 0 0 3.677 4.37a.07.07 0 0 0-.032.027C.533 9.046-.32 13.58.099 18.057a.082.082 0 0 0 .031.057 19.9 19.9 0 0 0 5.993 3.03.078.078 0 0 0 .084-.028 14.09 14.09 0 0 0 1.226-1.994.076.076 0 0 0-.041-.106 13.107 13.107 0 0 1-1.872-.892.077.077 0 0 1-.008-.128 10.2 10.2 0 0 0 .372-.292.074.074 0 0 1 .077-.01c3.928 1.793 8.18 1.793 12.062 0a.074.074 0 0 1 .078.01c.12.098.246.198.373.292a.077.077 0 0 1-.006.127 12.299 12.299 0 0 1-1.873.892.077.077 0 0 0-.041.107c.36.698.772 1.362 1.225 1.993a.076.076 0 0 0 .084.028 19.839 19.839 0 0 0 6.002-3.03.077.077 0 0 0 .032-.054c.5-5.177-.838-9.674-3.549-13.66a.061.061 0 0 0-.031-.03z"/></svg>
            Login with Discord
          </button>
        </div>
      </div>

      <div id="view-guilds" class="hidden">
        <div class="flex items-center justify-between mb-8">
          <div>
            <h1 class="font-display text-3xl font-semibold mb-1">Your servers</h1>
            <p class="text-text-secondary">Choose which server to manage.</p>
          </div>
          <a href={`${base}dashboard/account/`} class="btn-ghost text-sm">Account</a>
        </div>
        <div id="guild-list-skeleton" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
          <div class="skeleton-card"><div class="skeleton skeleton-text mb-3"></div><div class="skeleton skeleton-text-sm"></div></div>
          <div class="skeleton-card"><div class="skeleton skeleton-text mb-3"></div><div class="skeleton skeleton-text-sm"></div></div>
          <div class="skeleton-card"><div class="skeleton skeleton-text mb-3"></div><div class="skeleton skeleton-text-sm"></div></div>
        </div>
        <div id="guild-list" class="hidden grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4"></div>
      </div>

      <div id="view-bot-required" class="hidden"></div>

      <div id="view-overview" class="hidden">
        <div class="flex items-center justify-between mb-8">
          <div>
            <h1 class="font-display text-2xl font-semibold tracking-tight">Overview</h1>
            <p id="overview-subtitle" class="text-text-secondary text-sm mt-1"></p>
          </div>
        </div>

        <div class="grid grid-cols-2 sm:grid-cols-4 gap-4 mb-8">
          <div class="card">
            <p class="text-xs text-text-secondary mb-1 uppercase tracking-wide">Verified</p>
            <p id="stat-verified" class="text-3xl font-display font-semibold text-accent hidden">0</p>
            <div id="stat-verified-skeleton" class="skeleton skeleton-text-lg mt-2"></div>
          </div>
          <div class="card">
            <p class="text-xs text-text-secondary mb-1 uppercase tracking-wide">Pending</p>
            <p id="stat-pending" class="text-3xl font-display font-semibold text-warning hidden">0</p>
            <div id="stat-pending-skeleton" class="skeleton skeleton-text-lg mt-2"></div>
          </div>
          <div class="card">
            <p class="text-xs text-text-secondary mb-1 uppercase tracking-wide">Flagged</p>
            <p id="stat-flagged" class="text-3xl font-display font-semibold text-danger hidden">0</p>
            <div id="stat-flagged-skeleton" class="skeleton skeleton-text-lg mt-2"></div>
          </div>
          <div class="card">
            <p class="text-xs text-text-secondary mb-1 uppercase tracking-wide">Features</p>
            <p id="stat-features" class="text-lg font-medium text-text-primary mt-1 hidden">—</p>
            <div id="stat-features-skeleton" class="skeleton skeleton-text mt-2"></div>
          </div>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
          <div class="card">
            <h3 class="font-display font-semibold mb-4">Quick Actions</h3>
            <div class="flex flex-col gap-2">
              <a href="#members" class="nav-link flex items-center gap-3 px-3 py-2 rounded text-sm text-text-secondary hover:text-text-primary hover:bg-bg-surface-raised transition-colors">
                <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
                Manage Members
              </a>
              <a href="#settings" class="nav-link flex items-center gap-3 px-3 py-2 rounded text-sm text-text-secondary hover:text-text-primary hover:bg-bg-surface-raised transition-colors">
                <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>
                Server Settings
              </a>
              <a href="#features" class="nav-link flex items-center gap-3 px-3 py-2 rounded text-sm text-text-secondary hover:text-text-primary hover:bg-bg-surface-raised transition-colors">
                <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
                Features & Security
              </a>
            </div>
          </div>

          <div class="card">
            <h3 class="font-display font-semibold mb-4">Server Configuration</h3>
            <div id="config-summary" class="flex flex-col gap-3 text-sm">
              <div class="flex justify-between"><span class="text-text-secondary">Verified Role</span><span id="cfg-role-display" class="font-mono text-text-primary">—</span></div>
              <div class="flex justify-between"><span class="text-text-secondary">Verification Channel</span><span id="cfg-verification-display" class="font-mono text-text-primary">—</span></div>
              <div class="flex justify-between"><span class="text-text-secondary">Log Channel</span><span id="cfg-log-display" class="font-mono text-text-primary">—</span></div>
              <div class="flex justify-between"><span class="text-text-secondary">Mod Role</span><span id="cfg-mod-display" class="font-mono text-text-primary">—</span></div>
            </div>
          </div>
        </div>

        <div id="flagged-section" class="hidden mt-6">
          <div class="card">
            <div class="flex items-center justify-between mb-4">
              <h3 class="font-display font-semibold">Flagged Accounts</h3>
              <span class="pill pill-danger" id="flagged-count-badge">0</span>
            </div>
            <div id="flagged-items" class="flex flex-col"></div>
          </div>
        </div>
      </div>

      <div id="view-members" class="hidden">
        <div class="flex items-center justify-between mb-6">
          <div>
            <h1 class="font-display text-2xl font-semibold tracking-tight">Members</h1>
            <p class="text-text-secondary text-sm mt-1">Manage verified users in this server.</p>
          </div>
          <div class="flex gap-2">
            <button class="tab tab-active" data-tab="verified">Verified</button>
            <button class="tab" data-tab="pending">Pending Review</button>
          </div>
        </div>

        <div class="flex items-center gap-4 mb-6">
          <input type="text" id="member-search" placeholder="Search by Discord ID..." class="input max-w-sm" />
          <span id="member-count" class="text-sm text-text-secondary"></span>
        </div>

        <div id="tab-content-verified" class="hidden">
          <div id="members-empty" class="hidden text-text-secondary py-12 text-center">No verified members yet.</div>
          <div id="members-list" class="hidden flex flex-col"></div>
        </div>

        <div id="tab-content-pending" class="hidden">
          <div id="pending-empty" class="hidden text-text-secondary py-12 text-center">No pending reviews.</div>
          <div id="pending-list" class="hidden flex flex-col"></div>
        </div>

        <div id="members-skeleton" class="flex flex-col">
          <div class="table-row flex items-center gap-3 py-3 px-4"><div class="skeleton skeleton-circle w-8 h-8"></div><div class="flex-1"><div class="skeleton skeleton-text mb-1"></div><div class="skeleton skeleton-text-sm"></div></div></div>
          <div class="table-row flex items-center gap-3 py-3 px-4"><div class="skeleton skeleton-circle w-8 h-8"></div><div class="flex-1"><div class="skeleton skeleton-text mb-1"></div><div class="skeleton skeleton-text-sm"></div></div></div>
          <div class="table-row flex items-center gap-3 py-3 px-4"><div class="skeleton skeleton-circle w-8 h-8"></div><div class="flex-1"><div class="skeleton skeleton-text mb-1"></div><div class="skeleton skeleton-text-sm"></div></div></div>
          <div class="table-row flex items-center gap-3 py-3 px-4"><div class="skeleton skeleton-circle w-8 h-8"></div><div class="flex-1"><div class="skeleton skeleton-text mb-1"></div><div class="skeleton skeleton-text-sm"></div></div></div>
          <div class="table-row flex items-center gap-3 py-3 px-4"><div class="skeleton skeleton-circle w-8 h-8"></div><div class="flex-1"><div class="skeleton skeleton-text mb-1"></div><div class="skeleton skeleton-text-sm"></div></div></div>
        </div>
      </div>

      <div id="view-features" class="hidden">
        <div class="mb-8">
          <h1 class="font-display text-2xl font-semibold tracking-tight">Features & Security</h1>
          <p class="text-text-secondary text-sm mt-1">Configure verification requirements and security features.</p>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
          <div class="card">
            <div class="flex items-start gap-4">
              <div class="w-10 h-10 rounded-lg bg-accent/15 flex items-center justify-center flex-shrink-0">
                <svg class="w-5 h-5 text-accent" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
              </div>
              <div class="flex-1">
                <div class="flex items-center justify-between mb-2">
                  <h3 class="font-display font-semibold">Require Challenge</h3>
                  <label class="toggle"><input type="checkbox" id="feat-toggle-challenge" /><span class="toggle-slider"></span></label>
                </div>
                <p class="text-sm text-text-secondary">Re-run verification for every join, even for already-verified users. Use for high-risk servers.</p>
              </div>
            </div>
          </div>

          <div class="card">
            <div class="flex items-start gap-4">
              <div class="w-10 h-10 rounded-lg bg-accent/15 flex items-center justify-center flex-shrink-0">
                <svg class="w-5 h-5 text-accent" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M16 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="8.5" cy="7" r="4"/><polyline points="17 11 19 13 23 9"/></svg>
              </div>
              <div class="flex-1">
                <div class="flex items-center justify-between mb-2">
                  <h3 class="font-display font-semibold">Moderator Review</h3>
                  <label class="toggle"><input type="checkbox" id="feat-toggle-modreview" /><span class="toggle-slider"></span></label>
                </div>
                <p class="text-sm text-text-secondary">Require manual approval from a moderator before granting the verified role.</p>
                <div id="modreview-config" class="hidden mt-4 pt-4 border-t border-border-subtle">
                  <label class="block text-sm font-medium text-text-secondary mb-2">Review Channel ID</label>
                  <input type="text" id="cfg-modreview-channel" class="input" placeholder="Channel where users run /hlid" />
                </div>
              </div>
            </div>
          </div>

          <div class="card">
            <div class="flex items-start gap-4">
              <div class="w-10 h-10 rounded-lg bg-success/15 flex items-center justify-center flex-shrink-0">
                <svg class="w-5 h-5 text-success" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>
              </div>
              <div class="flex-1">
                <h3 class="font-display font-semibold mb-2">Always Active</h3>
                <div class="flex flex-col gap-2 text-sm">
                  <div class="flex items-center gap-2">
                    <span class="w-1.5 h-1.5 rounded-full bg-success"></span>
                    <span class="text-text-secondary">Disposable email detection</span>
                  </div>
                  <div class="flex items-center gap-2">
                    <span class="w-1.5 h-1.5 rounded-full bg-success"></span>
                    <span class="text-text-secondary">Cross-server identity</span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>

        <div class="mt-8 mb-6">
          <div class="flex items-center gap-2 mb-4">
            <h2 class="font-display text-lg font-semibold">Premium Features</h2>
            <span class="pill pill-accent">PRO</span>
          </div>
          <p class="text-text-secondary text-sm">Advanced features for power users. Requires a paid plan.</p>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
          <div class="card">
            <div class="flex items-start gap-4">
              <div class="w-10 h-10 rounded-lg bg-accent/15 flex items-center justify-center flex-shrink-0">
                <svg class="w-5 h-5 text-accent" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/></svg>
              </div>
              <div class="flex-1">
                <div class="flex items-center justify-between mb-2">
                  <h3 class="font-display font-semibold">Booster Bypass</h3>
                  <label class="toggle"><input type="checkbox" id="feat-toggle-booster" /><span class="toggle-slider"></span></label>
                </div>
                <p class="text-sm text-text-secondary">Skip verification for users currently boosting the server.</p>
              </div>
            </div>
          </div>

          <div class="card">
            <div class="flex items-start gap-4">
              <div class="w-10 h-10 rounded-lg bg-danger/15 flex items-center justify-center flex-shrink-0">
                <svg class="w-5 h-5 text-danger" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
              </div>
              <div class="flex-1">
                <div class="flex items-center justify-between mb-2">
                  <h3 class="font-display font-semibold">Raid Protection</h3>
                  <label class="toggle"><input type="checkbox" id="feat-toggle-raid" /><span class="toggle-slider"></span></label>
                </div>
                <p class="text-sm text-text-secondary">Monitor join rate and lock down on spikes.</p>
                <div id="raid-config" class="hidden mt-4 pt-4 border-t border-border-subtle">
                  <div class="grid grid-cols-2 gap-3">
                    <div>
                      <label class="block text-sm font-medium text-text-secondary mb-1">Window (seconds)</label>
                      <input type="number" id="cfg-raid-window" class="input" value="60" min="10" max="300" />
                    </div>
                    <div>
                      <label class="block text-sm font-medium text-text-secondary mb-1">Threshold</label>
                      <input type="number" id="cfg-raid-threshold" class="input" value="10" min="3" max="100" />
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <div class="card">
            <div class="flex items-start gap-4">
              <div class="w-10 h-10 rounded-lg bg-accent/15 flex items-center justify-center flex-shrink-0">
                <svg class="w-5 h-5 text-accent" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
              </div>
              <div class="flex-1">
                <div class="flex items-center justify-between mb-2">
                  <h3 class="font-display font-semibold">Audit Export</h3>
                  <label class="toggle"><input type="checkbox" id="feat-toggle-audit" /><span class="toggle-slider"></span></label>
                </div>
                <p class="text-sm text-text-secondary">Export verification events to your own audit trail.</p>
              </div>
            </div>
          </div>

          <div class="card">
            <div class="flex items-start gap-4">
              <div class="w-10 h-10 rounded-lg bg-accent/15 flex items-center justify-center flex-shrink-0">
                <svg class="w-5 h-5 text-accent" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
              </div>
              <div class="flex-1">
                <div class="flex items-center justify-between mb-2">
                  <h3 class="font-display font-semibold">Custom Timeout</h3>
                  <label class="toggle"><input type="checkbox" id="feat-toggle-timeout" /><span class="toggle-slider"></span></label>
                </div>
                <p class="text-sm text-text-secondary">Override the default verification link expiry time.</p>
                <div id="timeout-config" class="hidden mt-4 pt-4 border-t border-border-subtle">
                  <label class="block text-sm font-medium text-text-secondary mb-2">Timeout (minutes)</label>
                  <input type="number" id="cfg-timeout-minutes" class="input" value="10" min="1" max="60" />
                </div>
              </div>
            </div>
          </div>

          <div class="card">
            <div class="flex items-start gap-4">
              <div class="w-10 h-10 rounded-lg bg-accent/15 flex items-center justify-center flex-shrink-0">
                <svg class="w-5 h-5 text-accent" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/></svg>
              </div>
              <div class="flex-1">
                <div class="flex items-center justify-between mb-2">
                  <h3 class="font-display font-semibold">Vanity URL</h3>
                  <label class="toggle"><input type="checkbox" id="feat-toggle-vanity" /><span class="toggle-slider"></span></label>
                </div>
                <p class="text-sm text-text-secondary">Custom slug on hyprlane.qd.je/s/your-server.</p>
                <div id="vanity-config" class="hidden mt-4 pt-4 border-t border-border-subtle">
                  <label class="block text-sm font-medium text-text-secondary mb-2">Slug</label>
                  <div class="flex items-center gap-2">
                    <span class="text-text-muted text-sm">hyprlane.qd.je/s/</span>
                    <input type="text" id="cfg-vanity-slug" class="input flex-1" placeholder="my-server" />
                  </div>
                </div>
              </div>
            </div>
          </div>

          <div class="card">
            <div class="flex items-start gap-4">
              <div class="w-10 h-10 rounded-lg bg-accent/15 flex items-center justify-center flex-shrink-0">
                <svg class="w-5 h-5 text-accent" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
              </div>
              <div class="flex-1">
                <div class="flex items-center justify-between mb-2">
                  <h3 class="font-display font-semibold">Config Duplicate</h3>
                  <label class="toggle"><input type="checkbox" id="feat-toggle-duplicate" /><span class="toggle-slider"></span></label>
                </div>
                <p class="text-sm text-text-secondary">One-click copy settings to another server you own.</p>
              </div>
            </div>
          </div>

          <div class="card">
            <div class="flex items-start gap-4">
              <div class="w-10 h-10 rounded-lg bg-accent/15 flex items-center justify-center flex-shrink-0">
                <svg class="w-5 h-5 text-accent" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="4"/><line x1="1.05" y1="12" x2="7" y2="12"/><line x1="17.01" y1="12" x2="22.96" y2="12"/></svg>
              </div>
              <div class="flex-1">
                <div class="flex items-center justify-between mb-2">
                  <h3 class="font-display font-semibold">Custom Domain</h3>
                  <label class="toggle"><input type="checkbox" id="feat-toggle-domain" /><span class="toggle-slider"></span></label>
                </div>
                <p class="text-sm text-text-secondary">Use your own domain for verification links.</p>
                <div id="domain-config" class="hidden mt-4 pt-4 border-t border-border-subtle">
                  <label class="block text-sm font-medium text-text-secondary mb-2">Domain</label>
                  <input type="text" id="cfg-domain-name" class="input" placeholder="verify.yourserver.com" />
                </div>
              </div>
            </div>
          </div>

          <div class="card">
            <div class="flex items-start gap-4">
              <div class="w-10 h-10 rounded-lg bg-accent/15 flex items-center justify-center flex-shrink-0">
                <svg class="w-5 h-5 text-accent" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 11l3 3L22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>
              </div>
              <div class="flex-1">
                <div class="flex items-center justify-between mb-2">
                  <h3 class="font-display font-semibold">Custom Success Page</h3>
                  <label class="toggle"><input type="checkbox" id="feat-toggle-success" /><span class="toggle-slider"></span></label>
                </div>
                <p class="text-sm text-text-secondary">Customize the "you're verified" confirmation page.</p>
              </div>
            </div>
          </div>
        </div>

        <div class="mt-6 flex items-center gap-4">
          <div id="features-saved" class="hidden text-sm text-success">Features saved.</div>
          <button id="save-features-btn" class="btn-primary">Save feature settings</button>
        </div>
      </div>

      <div id="view-settings" class="hidden">
        <div class="mb-8">
          <h1 class="font-display text-2xl font-semibold tracking-tight">Settings</h1>
          <p class="text-text-secondary text-sm mt-1">Configure roles, channels, and general settings.</p>
        </div>

        <form id="settings-form" class="max-w-2xl flex flex-col gap-8">
          <div class="card">
            <h3 class="font-display font-semibold mb-4">Roles</h3>
            <div class="flex flex-col gap-4">
              <div>
                <label class="block text-sm font-medium text-text-secondary mb-2">Verified Role</label>
                <p class="text-xs text-text-muted mb-2">Role assigned to verified users</p>
                <input type="text" id="cfg-role" class="input" placeholder="Role ID" />
              </div>
              <div>
                <label class="block text-sm font-medium text-text-secondary mb-2">Mod Role</label>
                <p class="text-xs text-text-muted mb-2">Role allowed to use moderation commands</p>
                <input type="text" id="cfg-mod" class="input" placeholder="Optional" />
              </div>
            </div>
          </div>

          <div class="card">
            <h3 class="font-display font-semibold mb-4">Channels</h3>
            <div class="flex flex-col gap-4">
              <div>
                <label class="block text-sm font-medium text-text-secondary mb-2">Verification Channel</label>
                <p class="text-xs text-text-muted mb-2">Channel visible to unverified users with verification instructions</p>
                <input type="text" id="cfg-verification" class="input" placeholder="Channel ID" />
              </div>
              <div>
                <label class="block text-sm font-medium text-text-secondary mb-2">Log Channel</label>
                <p class="text-xs text-text-muted mb-2">Channel for audit logs and fallback notifications</p>
                <input type="text" id="cfg-log" class="input" placeholder="Optional" />
              </div>
            </div>
          </div>

          <div class="flex items-center gap-4">
            <div id="save-ok" class="hidden text-sm text-success">Settings saved.</div>
            <button type="submit" class="btn-primary">Save settings</button>
          </div>
        </form>
      </div>
    </main>
  </div>

  <script>
    import { createClient } from '@supabase/supabase-js';

    const API = 'https://hyprlane-api.sabplay-idk.workers.dev';
    const base = import.meta.env.BASE_URL;
    const supabaseUrl = 'https://reieovanxlrqgdqnsyfq.supabase.co';
    const supabaseAnonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJlaWVvdmFueGxycWdkcW5zeWZxIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODM3NzExMzgsImV4cCI6MjA5OTM0NzEzOH0.bOgC3L-z21lssiTgiPDzEMI9LiLhQJRTzczmkDnGGJY';
    const supabase = createClient(supabaseUrl, supabaseAnonKey, {
      auth: { flowType: 'implicit' },
    });

    let session: any = null;
    let guildId: string | null = null;
    let guildName: string = '';
    let allMembers: any[] = [];
    let allPending: any[] = [];
    let currentConfig: any = null;

    function authHeaders() {
      return {
        Authorization: `Bearer ${session?.access_token}`,
        'X-Discord-Token': session?.provider_token || '',
      };
    }

    function showView(name: string) {
      document.querySelectorAll('[id^="view-"]').forEach(el => el.classList.add('hidden'));
      document.getElementById(`view-${name}`)?.classList.remove('hidden');
    }

    function buildNav(gid: string) {
      const nav = document.getElementById('sidebar-nav')!;
      const items = [
        { id: 'overview', label: 'Overview', icon: '<svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/></svg>' },
        { id: 'members', label: 'Members', icon: '<svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>' },
        { id: 'features', label: 'Features', icon: '<svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>' },
        { id: 'settings', label: 'Settings', icon: '<svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>' },
      ];
      const currentHash = window.location.hash.replace('#', '');
      nav.innerHTML = items.map(v => {
        const active = v.sub === currentHash || (!v.sub && !currentHash);
        return `<a href="#${v.sub}" data-view="${v.id}" class="nav-link flex items-center gap-3 px-3 py-2 rounded text-sm transition-colors ${active ? 'bg-accent/10 text-accent' : 'text-text-secondary hover:text-text-primary hover:bg-bg-surface-raised'}">${v.icon}${v.label}</a>`;
      }).join('');
      nav.querySelectorAll('.nav-link').forEach(link => {
        link.addEventListener('click', (e) => {
          e.preventDefault();
          const view = (e.currentTarget as HTMLElement).dataset.view!;
          const sub = (e.currentTarget as HTMLAnchorElement).hash.replace('#', '');
          window.history.replaceState({}, '', `${base}dashboard/${gid}/${sub ? sub + '/' : ''}`);
          activateView(view);
        });
      });
    }

    function activateView(view: string) {
      showView(view);
      document.querySelectorAll('.nav-link').forEach(el => {
        const isActive = el.getAttribute('data-view') === view;
        el.classList.toggle('bg-accent/10', isActive);
        el.classList.toggle('text-accent', isActive);
        el.classList.toggle('text-text-secondary', !isActive);
      });
      if (view === 'overview') loadOverview();
      if (view === 'members') loadMembers();
      if (view === 'features') loadFeatures();
      if (view === 'settings') loadSettings();
    }

    async function parseUrl() {
      // Check for ?guild= query param (from 404.html redirect)
      const params = new URLSearchParams(window.location.search);
      const guildParam = params.get('guild');
      if (guildParam && guildParam !== '') {
        // Validate access before showing dashboard
        if (await validateGuildAccess(guildParam)) {
          guildId = guildParam;
          document.getElementById('sidebar')!.classList.remove('hidden');
          document.getElementById('main')!.classList.add('ml-56');
          buildNav(guildId);
          loadGuildInfo(guildId);

          // Check if bot is in the guild
          if (!(await checkBotInGuild(guildParam))) {
            showAddBotPrompt(guildParam);
            return null;
          }

          window.history.replaceState({}, '', `${base}dashboard/${guildId}/`);
          return 'overview';
        } else {
          // No access — redirect to server list
          window.history.replaceState({}, '', `${base}dashboard/`);
          return null;
        }
      }

      // Check for path-based routing
      const path = window.location.pathname.replace(base, '');
      const parts = path.split('/').filter(Boolean);
      if (parts.length >= 2 && parts[0] === 'dashboard') {
        const potentialGuildId = parts[1];
        // Validate access before showing dashboard
        if (await validateGuildAccess(potentialGuildId)) {
          guildId = potentialGuildId;
          document.getElementById('sidebar')!.classList.remove('hidden');
          document.getElementById('main')!.classList.add('ml-56');
          buildNav(guildId);
          loadGuildInfo(guildId);

          // Check if bot is in the guild
          if (!(await checkBotInGuild(potentialGuildId))) {
            showAddBotPrompt(potentialGuildId);
            return null;
          }

          const sub = parts[2] || '';
          if (sub === 'members') return 'members';
          if (sub === 'features') return 'features';
          if (sub === 'settings') return 'settings';
          return 'overview';
        } else {
          // No access — redirect to server list
          window.history.replaceState({}, '', `${base}dashboard/`);
          return null;
        }
      }
      return null;
    }

    async function loadGuildInfo(gid: string) {
      try {
        const res = await fetch(`${API}/dashboard/guilds`, { headers: authHeaders() });
        if (!res.ok) return;
        const guilds = await res.json();
        const guild = guilds.find((g: any) => g.id === gid);
        if (!guild) return;

        const header = document.getElementById('guild-header')!;
        const icon = document.getElementById('guild-icon')!;
        const name = document.getElementById('guild-name')!;
        const idDisplay = document.getElementById('guild-id-display')!;

        header.classList.remove('hidden');

        if (guild.icon) {
          icon.innerHTML = `<img src="https://cdn.discordapp.com/icons/${guild.id}/${guild.icon}.png" class="w-8 h-8 rounded-full" alt="" />`;
          icon.className = 'w-8 h-8 rounded-full flex-shrink-0';
        } else {
          icon.textContent = guild.name.charAt(0);
          icon.className = 'w-8 h-8 rounded-full bg-accent/15 flex items-center justify-center text-accent font-display text-sm font-semibold flex-shrink-0';
        }

        name.textContent = guild.name;
        idDisplay.textContent = guild.id;
      } catch {}
    }

    async function validateGuildAccess(guildIdToCheck: string): Promise<boolean> {
      if (!session) return false;
      try {
        const res = await fetch(`${API}/dashboard/guilds`, { headers: authHeaders() });
        if (!res.ok) return false;
        const guilds = await res.json();
        return guilds.some((g: any) => g.id === guildIdToCheck);
      } catch {
        return false;
      }
    }

    async function checkBotInGuild(guildIdToCheck: string): Promise<boolean> {
      if (!session) return false;
      try {
        const res = await fetch(`${API}/dashboard/guilds/${guildIdToCheck}/bot-status`, { headers: authHeaders() });
        if (!res.ok) return true; // Assume yes on error
        const data = await res.json();
        return data.in_guild;
      } catch {
        return true; // Assume yes on network error
      }
    }

    function showAddBotPrompt(guildIdToCheck: string) {
      const botClientId = '1524437831823523850';
      // Permissions: Manage Channels + Manage Roles + Send Messages + Embed Links + View Channels + Use Application Commands
      const inviteUrl = `https://discord.com/oauth2/authorize?client_id=${botClientId}&permissions=2416035840&integration_type=0&scope=bot+applications.commands`;

      // Hide sidebar, show bot-required view
      document.getElementById('sidebar')!.classList.add('hidden');
      document.getElementById('main')!.classList.remove('ml-56');

      document.getElementById('view-bot-required')!.classList.remove('hidden');
      document.getElementById('view-bot-required')!.innerHTML = `
        <div class="flex flex-col items-center justify-center py-16">
          <div class="w-16 h-16 rounded-2xl bg-warning/10 flex items-center justify-center mb-6">
            <svg class="w-8 h-8 text-warning" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L4.082 16.5c-.77.833.192 2.5 1.732 2.5z" />
            </svg>
          </div>
          <h2 class="font-display text-xl font-semibold mb-2">Bot Not in Server</h2>
          <p class="text-text-secondary text-center max-w-md mb-8">
            The Hyprlane bot needs to be added to this server before you can manage it.
          </p>
          <a href="${inviteUrl}" target="_blank" rel="noopener noreferrer" class="btn-primary px-6 py-3 text-sm">
            Add Bot to Server
          </a>
          <p class="text-text-muted text-xs mt-4">You need Manage Server permission to add the bot</p>
        </div>
      `;
    }

    supabase.auth.onAuthStateChange((event, s) => {
      if (event === 'SIGNED_IN' && s) {
        session = s;
        if (window.location.hash.includes('access_token')) {
          window.history.replaceState({}, '', `${base}dashboard/`);
        }
        // Re-run checkSession to handle guild param
        checkSession();
      } else if (event === 'SIGNED_OUT') {
        session = null;
        showView('login');
      }
    });

    checkSession();

    async function checkSession() {
      const { data: { session: s } } = await supabase.auth.getSession();
      session = s;

      const initial = await parseUrl();
      if (initial && session) {
        showView('overview');
        activateView(initial);
      } else if (session && !initial) {
        // Logged in but no valid guild in URL — show server list
        showView('guilds');
        loadGuilds();
      } else if (!session) {
        showView('login');
      }
    }

    document.getElementById('login-btn')?.addEventListener('click', async () => {
      const redir = `${window.location.origin}${base}dashboard/`;
      const { error } = await supabase.auth.signInWithOAuth({
        provider: 'discord',
        options: {
          redirectTo: redir,
          scopes: 'identify email guilds',
        },
      });
      if (error) {
        console.error('Login failed:', error);
      }
    });

    async function loadGuilds() {
      if (!session) return;
      document.getElementById('guild-list-skeleton')!.classList.remove('hidden');
      document.getElementById('guild-list')!.classList.add('hidden');
      try {
        const res = await fetch(`${API}/dashboard/guilds`, {
          headers: authHeaders(),
        });
        if (!res.ok) {
          console.error('loadGuilds failed:', res.status, await res.text());
          return;
        }
        const guilds = await res.json();
        document.getElementById('guild-list-skeleton')!.classList.add('hidden');
        document.getElementById('guild-list')!.classList.remove('hidden');
        document.getElementById('guild-list')!.innerHTML = guilds.map((g: any) => `
          <a href="${base}dashboard/${g.id}/" class="card group hover:border-accent transition-all hover:scale-[1.02]">
            <div class="flex items-center gap-4 mb-3">
              ${g.icon ? `<img src="https://cdn.discordapp.com/icons/${g.id}/${g.icon}.png" class="w-12 h-12 rounded-full" alt="" />` : `<div class="w-12 h-12 rounded-full bg-accent/15 flex items-center justify-center text-accent font-display text-lg font-semibold">${g.name.charAt(0)}</div>`}
              <div class="min-w-0">
                <p class="font-medium truncate group-hover:text-accent transition-colors">${g.name}</p>
                <p class="text-xs text-text-muted font-mono">ID: ${g.id}</p>
              </div>
            </div>
            <div class="flex items-center gap-2 text-xs text-text-secondary">
              <span class="pill">Manage</span>
              <svg class="w-3 h-3" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 18l6-6-6-6"/></svg>
            </div>
          </a>
        `).join('');
      } catch (e) {
        console.error('loadGuilds error:', e);
        document.getElementById('guild-list-skeleton')!.classList.add('hidden');
      }
    }

    async function loadOverview() {
      if (!session || !guildId) return;
      // Set subtitle from guild header
      const guildName = document.getElementById('guild-name')?.textContent;
      if (guildName) {
        document.getElementById('overview-subtitle')!.textContent = guildName;
      }
      try {
        const [stats, config] = await Promise.all([
          fetch(`${API}/dashboard/guilds/${guildId}/stats`, { headers: authHeaders() }).then(r => r.json()),
          fetch(`${API}/dashboard/guilds/${guildId}/config`, { headers: authHeaders() }).then(r => r.json()),
        ]);

        currentConfig = config;

        // Hide skeletons, show stats
        document.getElementById('stat-verified-skeleton')!.classList.add('hidden');
        document.getElementById('stat-pending-skeleton')!.classList.add('hidden');
        document.getElementById('stat-flagged-skeleton')!.classList.add('hidden');
        document.getElementById('stat-features-skeleton')!.classList.add('hidden');

        document.getElementById('stat-verified')!.classList.remove('hidden');
        document.getElementById('stat-pending')!.classList.remove('hidden');
        document.getElementById('stat-flagged')!.classList.remove('hidden');
        document.getElementById('stat-features')!.classList.remove('hidden');

        document.getElementById('stat-verified')!.textContent = String(stats.verified_count ?? 0);
        document.getElementById('stat-pending')!.textContent = String(stats.pending_count ?? 0);
        document.getElementById('stat-flagged')!.textContent = String(stats.flagged_count ?? 0);

        const f = config.enrolled_features || [];
        document.getElementById('stat-features')!.textContent = f.length ? f.join(', ') : 'Default';

        // Config summary
        document.getElementById('cfg-role-display')!.textContent = config.verified_role_id || 'Not set';
        document.getElementById('cfg-verification-display')!.textContent = config.verification_channel_id || 'None';
        document.getElementById('cfg-log-display')!.textContent = config.log_channel_id || 'None';
        document.getElementById('cfg-mod-display')!.textContent = config.mod_role_id || 'None';

        // Flagged accounts
        if (stats.flagged_count > 0) {
          const flaggedRes = await fetch(`${API}/dashboard/guilds/${guildId}/flagged-members`, { headers: authHeaders() });
          if (flaggedRes.ok) {
            const flagged = await flaggedRes.json();
            const section = document.getElementById('flagged-section')!;
            const items = document.getElementById('flagged-items')!;
            const badge = document.getElementById('flagged-count-badge')!;
            if (flagged.length > 0) {
              section.classList.remove('hidden');
              badge.textContent = String(flagged.length);
              items.innerHTML = flagged.map((m: any) => `
                <div class="flex items-center justify-between py-3 border-b border-border-subtle last:border-0">
                  <div class="flex items-center gap-3">
                    <div class="w-8 h-8 rounded-full bg-danger/15 flex items-center justify-center text-danger text-xs font-mono">${m.discord_id.slice(-4)}</div>
                    <div>
                      <p class="text-sm font-medium font-mono">${m.discord_id}</p>
                      <p class="text-xs text-text-muted">${m.flagged_reason || 'unknown'}</p>
                    </div>
                  </div>
                  <span class="pill pill-danger">${m.status}</span>
                </div>
              `).join('');
            }
          }
        }
      } catch {}
    }

    async function loadMembers() {
      if (!session || !guildId) return;
      // Show skeleton
      document.getElementById('members-skeleton')!.classList.remove('hidden');
      document.getElementById('tab-content-verified')!.classList.add('hidden');
      document.getElementById('tab-content-pending')!.classList.add('hidden');
      try {
        const res = await fetch(`${API}/dashboard/guilds/${guildId}/verified-members`, {
          headers: authHeaders(),
        });
        const members = await res.json();
        allMembers = members.filter((m: any) => m.guild_overrides?.[guildId]?.local_status !== 'pending_mod_review');
        allPending = members.filter((m: any) => m.guild_overrides?.[guildId]?.local_status === 'pending_mod_review');
        // Hide skeleton
        document.getElementById('members-skeleton')!.classList.add('hidden');
        renderMembers(allMembers);
        renderPending(allPending);
        updateMemberCount();
      } catch {
        document.getElementById('members-skeleton')!.classList.add('hidden');
      }
    }

    function updateMemberCount() {
      const q = (document.getElementById('member-search') as HTMLInputElement)?.value?.toLowerCase() || '';
      const filtered = currentTab === 'verified'
        ? allMembers.filter(m => m.discord_id.includes(q))
        : allPending.filter(m => m.discord_id.includes(q));
      document.getElementById('member-count')!.textContent = `${filtered.length} ${currentTab === 'verified' ? 'verified' : 'pending'}`;
    }

    function renderMembers(members: any[]) {
      const empty = document.getElementById('members-empty')!;
      const list = document.getElementById('members-list')!;
      if (!members.length) { empty.classList.remove('hidden'); list.classList.add('hidden'); return; }
      empty.classList.add('hidden'); list.classList.remove('hidden');
      list.innerHTML = members.map(m => `
        <div class="table-row flex items-center justify-between py-3 px-4">
          <div class="flex items-center gap-3 min-w-0">
            <div class="w-8 h-8 rounded-full bg-bg-surface-raised flex items-center justify-center text-text-muted text-xs font-mono flex-shrink-0">${m.discord_id.slice(-4)}</div>
            <div class="min-w-0">
              <p class="text-sm font-medium font-mono truncate">${m.discord_id}</p>
              <p class="text-xs text-text-muted">${m.verified_at ? `Verified ${new Date(m.verified_at).toLocaleDateString()}` : '—'} · ${m.method || 'oauth'}</p>
            </div>
          </div>
          <div class="flex items-center gap-3 flex-shrink-0">
            <button class="btn-danger-sm revoke-btn" data-id="${m.discord_id}">Revoke</button>
          </div>
        </div>
      `).join('');

      list.querySelectorAll('.revoke-btn').forEach(btn => {
        btn.addEventListener('click', async (e) => {
          const id = (e.currentTarget as HTMLElement).dataset.id!;
          if (!confirm(`Revoke verification for ${id}?`)) return;
          await fetch(`${API}/dashboard/guilds/${guildId}/members/${id}/revoke`, {
            method: 'POST',
            headers: authHeaders(),
          });
          loadMembers();
          loadOverview();
        });
      });
    }

    function renderPending(members: any[]) {
      const empty = document.getElementById('pending-empty')!;
      const list = document.getElementById('pending-list')!;
      if (!members.length) { empty.classList.remove('hidden'); list.classList.add('hidden'); return; }
      empty.classList.add('hidden'); list.classList.remove('hidden');
      list.innerHTML = members.map(m => `
        <div class="table-row flex items-center justify-between py-3 px-4">
          <div class="flex items-center gap-3 min-w-0">
            <div class="w-8 h-8 rounded-full bg-warning/15 flex items-center justify-center text-warning text-xs font-mono flex-shrink-0">${m.discord_id.slice(-4)}</div>
            <div class="min-w-0">
              <p class="text-sm font-medium font-mono truncate">${m.discord_id}</p>
              <p class="text-xs text-text-muted">Awaiting moderator review</p>
            </div>
          </div>
          <div class="flex items-center gap-2 flex-shrink-0">
            <button class="btn-success-sm approve-btn" data-id="${m.discord_id}">Approve</button>
            <button class="btn-danger-sm reject-btn" data-id="${m.discord_id}">Reject</button>
          </div>
        </div>
      `).join('');

      list.querySelectorAll('.approve-btn').forEach(btn => {
        btn.addEventListener('click', async (e) => {
          const id = (e.currentTarget as HTMLElement).dataset.id!;
          await fetch(`${API}/dashboard/guilds/${guildId}/members/${id}/setstatus`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json', ...authHeaders() },
            body: JSON.stringify({ status: 'verified' }),
          });
          loadMembers();
          loadOverview();
        });
      });

      list.querySelectorAll('.reject-btn').forEach(btn => {
        btn.addEventListener('click', async (e) => {
          const id = (e.currentTarget as HTMLElement).dataset.id!;
          if (!confirm(`Reject and unverify ${id}?`)) return;
          await fetch(`${API}/dashboard/guilds/${guildId}/members/${id}/setstatus`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json', ...authHeaders() },
            body: JSON.stringify({ status: 'unverified' }),
          });
          loadMembers();
          loadOverview();
        });
      });
    }

    // Tab switching
    let currentTab = 'verified';
    document.querySelectorAll('.tab').forEach(tab => {
      tab.addEventListener('click', (e) => {
        currentTab = (e.currentTarget as HTMLElement).dataset.tab!;
        document.querySelectorAll('.tab').forEach(t => t.classList.toggle('tab-active', t === e.currentTarget));
        document.getElementById('tab-content-verified')!.classList.toggle('hidden', currentTab !== 'verified');
        document.getElementById('tab-content-pending')!.classList.toggle('hidden', currentTab !== 'pending');
        updateMemberCount();
      });
    });

    document.getElementById('member-search')?.addEventListener('input', (e) => {
      const q = (e.target as HTMLInputElement).value.toLowerCase();
      renderMembers(allMembers.filter(m => m.discord_id.includes(q)));
      renderPending(allPending.filter(m => m.discord_id.includes(q)));
      updateMemberCount();
    });

    async function loadFeatures() {
      if (!session || !guildId) return;
      try {
        const c = await fetch(`${API}/dashboard/guilds/${guildId}/config`, {
          headers: authHeaders(),
        }).then(r => r.json());
        currentConfig = c;
        const f = c.enrolled_features || [];
        const fc = c.feature_config || {};
        (document.getElementById('feat-toggle-challenge') as HTMLInputElement).checked = f.includes('require_challenge');
        (document.getElementById('feat-toggle-modreview') as HTMLInputElement).checked = f.includes('mod_review');
        (document.getElementById('feat-toggle-booster') as HTMLInputElement).checked = f.includes('booster_bypass');
        (document.getElementById('feat-toggle-raid') as HTMLInputElement).checked = f.includes('raid_protection');
        (document.getElementById('feat-toggle-audit') as HTMLInputElement).checked = f.includes('audit_export');
        (document.getElementById('feat-toggle-timeout') as HTMLInputElement).checked = f.includes('custom_timeout');
        (document.getElementById('feat-toggle-vanity') as HTMLInputElement).checked = f.includes('vanity_url');
        (document.getElementById('feat-toggle-duplicate') as HTMLInputElement).checked = f.includes('config_duplicate');
        (document.getElementById('feat-toggle-domain') as HTMLInputElement).checked = f.includes('custom_verification_domain');
        (document.getElementById('feat-toggle-success') as HTMLInputElement).checked = f.includes('custom_success_page');

        const modreviewConfig = document.getElementById('modreview-config')!;
        if (f.includes('mod_review')) {
          modreviewConfig.classList.remove('hidden');
          (document.getElementById('cfg-modreview-channel') as HTMLInputElement).value = fc.mod_review?.channel_id || '';
        } else {
          modreviewConfig.classList.add('hidden');
        }

        const raidConfig = document.getElementById('raid-config')!;
        if (f.includes('raid_protection')) {
          raidConfig.classList.remove('hidden');
          (document.getElementById('cfg-raid-window') as HTMLInputElement).value = String(fc.raid_protection?.window_seconds || 60);
          (document.getElementById('cfg-raid-threshold') as HTMLInputElement).value = String(fc.raid_protection?.threshold || 10);
        } else {
          raidConfig.classList.add('hidden');
        }

        const timeoutConfig = document.getElementById('timeout-config')!;
        if (f.includes('custom_timeout')) {
          timeoutConfig.classList.remove('hidden');
          (document.getElementById('cfg-timeout-minutes') as HTMLInputElement).value = String(fc.custom_timeout?.minutes || 10);
        } else {
          timeoutConfig.classList.add('hidden');
        }

        const vanityConfig = document.getElementById('vanity-config')!;
        if (f.includes('vanity_url')) {
          vanityConfig.classList.remove('hidden');
          (document.getElementById('cfg-vanity-slug') as HTMLInputElement).value = fc.vanity_url?.slug || '';
        } else {
          vanityConfig.classList.add('hidden');
        }

        const domainConfig = document.getElementById('domain-config')!;
        if (f.includes('custom_verification_domain')) {
          domainConfig.classList.remove('hidden');
          (document.getElementById('cfg-domain-name') as HTMLInputElement).value = fc.custom_verification_domain?.domain || '';
        } else {
          domainConfig.classList.add('hidden');
        }
      } catch {}
    }

    document.getElementById('feat-toggle-modreview')?.addEventListener('change', (e) => {
      const checked = (e.target as HTMLInputElement).checked;
      document.getElementById('modreview-config')!.classList.toggle('hidden', !checked);
    });

    document.getElementById('feat-toggle-raid')?.addEventListener('change', (e) => {
      const checked = (e.target as HTMLInputElement).checked;
      document.getElementById('raid-config')!.classList.toggle('hidden', !checked);
    });

    document.getElementById('feat-toggle-timeout')?.addEventListener('change', (e) => {
      const checked = (e.target as HTMLInputElement).checked;
      document.getElementById('timeout-config')!.classList.toggle('hidden', !checked);
    });

    document.getElementById('feat-toggle-vanity')?.addEventListener('change', (e) => {
      const checked = (e.target as HTMLInputElement).checked;
      document.getElementById('vanity-config')!.classList.toggle('hidden', !checked);
    });

    document.getElementById('feat-toggle-domain')?.addEventListener('change', (e) => {
      const checked = (e.target as HTMLInputElement).checked;
      document.getElementById('domain-config')!.classList.toggle('hidden', !checked);
    });

    document.getElementById('save-features-btn')?.addEventListener('click', async () => {
      if (!session || !guildId) return;
      const f: string[] = [];
      if ((document.getElementById('feat-toggle-challenge') as HTMLInputElement).checked) f.push('require_challenge');
      if ((document.getElementById('feat-toggle-modreview') as HTMLInputElement).checked) f.push('mod_review');
      if ((document.getElementById('feat-toggle-booster') as HTMLInputElement).checked) f.push('booster_bypass');
      if ((document.getElementById('feat-toggle-raid') as HTMLInputElement).checked) f.push('raid_protection');
      if ((document.getElementById('feat-toggle-audit') as HTMLInputElement).checked) f.push('audit_export');
      if ((document.getElementById('feat-toggle-timeout') as HTMLInputElement).checked) f.push('custom_timeout');
      if ((document.getElementById('feat-toggle-vanity') as HTMLInputElement).checked) f.push('vanity_url');
      if ((document.getElementById('feat-toggle-duplicate') as HTMLInputElement).checked) f.push('config_duplicate');
      if ((document.getElementById('feat-toggle-domain') as HTMLInputElement).checked) f.push('custom_verification_domain');
      if ((document.getElementById('feat-toggle-success') as HTMLInputElement).checked) f.push('custom_success_page');

      const featureConfig: any = currentConfig.feature_config || {};
      if (f.includes('mod_review')) {
        featureConfig.mod_review = { channel_id: (document.getElementById('cfg-modreview-channel') as HTMLInputElement).value };
      }
      if (f.includes('raid_protection')) {
        featureConfig.raid_protection = {
          window_seconds: parseInt((document.getElementById('cfg-raid-window') as HTMLInputElement).value) || 60,
          threshold: parseInt((document.getElementById('cfg-raid-threshold') as HTMLInputElement).value) || 10,
        };
      }
      if (f.includes('custom_timeout')) {
        featureConfig.custom_timeout = { minutes: parseInt((document.getElementById('cfg-timeout-minutes') as HTMLInputElement).value) || 10 };
      }
      if (f.includes('vanity_url')) {
        featureConfig.vanity_url = { slug: (document.getElementById('cfg-vanity-slug') as HTMLInputElement).value };
      }
      if (f.includes('custom_verification_domain')) {
        featureConfig.custom_verification_domain = { domain: (document.getElementById('cfg-domain-name') as HTMLInputElement).value };
      }

      await fetch(`${API}/dashboard/guilds/${guildId}/config`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json', ...authHeaders() },
        body: JSON.stringify({
          ...currentConfig,
          enrolled_features: f,
          feature_config: featureConfig,
        }),
      });

      const ok = document.getElementById('features-saved')!;
      ok.classList.remove('hidden');
      setTimeout(() => ok.classList.add('hidden'), 2000);
    });

    async function loadSettings() {
      if (!session || !guildId) return;
      try {
        const c = await fetch(`${API}/dashboard/guilds/${guildId}/config`, {
          headers: authHeaders(),
        }).then(r => r.json());
        currentConfig = c;
        (document.getElementById('cfg-role') as HTMLInputElement).value = c.verified_role_id || '';
        (document.getElementById('cfg-log') as HTMLInputElement).value = c.log_channel_id || '';
        (document.getElementById('cfg-mod') as HTMLInputElement).value = c.mod_role_id || '';
        (document.getElementById('cfg-verification') as HTMLInputElement).value = c.verification_channel_id || '';
      } catch {}
    }

    document.getElementById('settings-form')?.addEventListener('submit', async (e) => {
      e.preventDefault();
      if (!session || !guildId) return;

      await fetch(`${API}/dashboard/guilds/${guildId}/config`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json', ...authHeaders() },
        body: JSON.stringify({
          ...currentConfig,
          verified_role_id: (document.getElementById('cfg-role') as HTMLInputElement).value,
          log_channel_id: (document.getElementById('cfg-log') as HTMLInputElement).value || null,
          mod_role_id: (document.getElementById('cfg-mod') as HTMLInputElement).value || null,
          verification_channel_id: (document.getElementById('cfg-verification') as HTMLInputElement).value || null,
        }),
      });

      const ok = document.getElementById('save-ok')!;
      ok.classList.remove('hidden');
      setTimeout(() => ok.classList.add('hidden'), 2000);
    });
  </script>
</Layout>
